server:
  port: 8001

spring:
  application:
    name: EventBuddy

eureka:
  client:
    register-with-client: true
    fetch-registry: true
    service-url:
      default:zone: http://localhost:8761/eureka/
    instance:
      hostname: localhost